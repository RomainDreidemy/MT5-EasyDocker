import { expect, test } from 'vitest'
import BaseCanvas from './Base.canvas'

test('should be possible to get call base canvas', async () => {
  expect(BaseCanvas).toBeTruthy()
})

test('should be possible to create a canvas', async () => {
})
