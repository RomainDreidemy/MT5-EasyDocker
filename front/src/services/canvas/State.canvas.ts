import {TEventsCanvas} from "../../types/canvas/Events.canvas";
import {TStateCanvas} from "../../types/canvas/State.Canvas";

const StateCanvas: TStateCanvas = {
  drawers: [],
  isMoving: false
}

export default StateCanvas